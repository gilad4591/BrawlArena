# Copy to `.env` and set your deployed relay URL to enable cross-device
# multiplayer. Leave unset for same-device (BroadcastChannel) testing.
#
# Local:  ws://localhost:8787
# Prod:   wss://your-relay-host.example.com
VITE_MP_RELAY_URL=
