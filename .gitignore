# Dependencies
node_modules/

# Build output
dist/
*.log

# Editor / OS
.DS_Store
.vscode/
.idea/
*.iml

# Capacitor generated web copy
android/app/src/main/assets/public/

# Android / Gradle build artifacts
android/.gradle/
android/build/
android/app/build/
android/capacitor-cordova-android-plugins/build/
android/local.properties
.gradle/
*.apk
*.aab
*.keystore
*.jks
android/keystore.properties

# iOS (if added later)
ios/App/Pods/
ios/App/build/

# iOS signing material generated locally by store/ios/generate-csr.ps1 and
# cert-to-p12.ps1 -- private key + cert artifacts, never commit these.
store/ios/*.key
store/ios/*.csr
store/ios/*.cer
store/ios/*.pem
store/ios/*.p12
store/ios/*.mobileprovision

# Env / secrets
.env
.env.local

# grid-sheet verification montages (throwaway)
scripts/raw/*_check.png
scripts/raw/portrait_check.png
scripts/raw/_commitmsg.txt

# responsive/layout screenshot captures (throwaway)
scripts/raw/shots/

# Throwaway mobile-render check screenshot
scripts/social/mobcheck.png
